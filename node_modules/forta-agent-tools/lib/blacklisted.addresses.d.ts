import { FindingGenerator } from "./utils";
import { HandleTransaction } from "forta-agent";
export default function provideBlacklistedAddresessHandler(findingGenerator: FindingGenerator, blacklistedAddresses: string[]): HandleTransaction;
