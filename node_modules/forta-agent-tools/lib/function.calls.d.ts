import { HandleTransaction } from "forta-agent";
import { FindingGenerator } from "./utils";
import { AbiItem } from "web3-utils";
interface AgentOptions {
    from?: string;
    to?: string;
}
declare type Signature = string | AbiItem;
export default function provideFunctionCallsDetectorHandler(findingGenerator: FindingGenerator, functionSignature: Signature, agentOptions?: AgentOptions): HandleTransaction;
export {};
