/// <reference types="node" />
import fs from 'fs';
import shelljs from "shelljs";
import prompts from "prompts";
import { GetKeyfile } from '../../utils/get.keyfile';
export declare type GetCredentials = () => Promise<{
    publicKey: string;
    privateKey: string;
}>;
export default function provideGetCredentials(shell: typeof shelljs, prompt: typeof prompts, filesystem: typeof fs, getKeyfile: GetKeyfile, fortaKeystore: string, keyfileName?: string): GetCredentials;
